# Required: OpenAI API key (used for TTS and optionally for translation)
OPENAI_API_KEY=sk-...

# Optional: Groq API key (if set, will be used for translation instead of OpenAI — faster and cheaper)
GROQ_API_KEY=gsk_...

# Optional: Server port (default: 3001)
PORT=3001
